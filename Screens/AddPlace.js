import React from "react";
import { View, Text } from "react-native";

const AddPlace = () => {
  return (
    <View>
      <Text>This is Add place page</Text>
    </View>
  );
};

export default AddPlace;
